{
  "solution_code": "# telegram_bot/bot.py\nimport os\nimport logging\nfrom telegram import Update, WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup\nfrom telegram.ext import Application, CommandHandler, ContextTypes, MessageHandler, filters\nimport httpx\nimport json\n\n# Configure logging\nlogging.basicConfig(\n    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',\n    level=logging.INFO\n)\nlogger = logging.getLogger(__name__)\n\n# Configuration\nTELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', '')\nMCP_SERVER_URL = os.getenv('MCP_SERVER_URL', 'http://localhost:8000')\nWEBAPP_URL = os.getenv('WEBAPP_URL', 'https://your-domain.com/telegram-miniapp.html')\n\nclass RobinMCPBot:\n    def __init__(self):\n        self.app = Application.builder().token(TELEGRAM_BOT_TOKEN).build()\n        self.http_client = httpx.AsyncClient(timeout=30.0)\n        \n    async def start(self, update: Update, context: ContextTypes.DEFAULT_TYPE):\n        \"\"\"Send welcome message with Mini-App button.\"\"\"\n        keyboard = [[\n            InlineKeyboardButton(\n                \"🚀 Launch Mini-App\",\n                web_app=WebAppInfo(url=WEBAPP_URL)\n            )\n        ]]\n        reply_markup = InlineKeyboardMarkup(keyboard)\n        \n        welcome_text = (\n            \"🏦 *Welcome to Robinhood Chain MCP Bot!*\\n\\n\"\n            \"Launch meme coins, track community trusts, and manage your portfolio.\\n\\n\"\n            \"*Commands:*\\n\"\n            \"/launch - Deploy a new token\\n\"\n            \"/trust - Deploy a community trust\\n\"\n            \"/reserves - View gold/silver reserves\\n\"\n            \"/portfolio - Check your holdings\\n\"\n            \"/webapp - Open Mini-App interface\\n\\n\"\n            \"Click the button below to open the full Mini-App! 👇\"\n        )\n        \n        await update.message.reply_text(\n            welcome_text,\n            parse_mode='Markdown',\n            reply_markup=reply_markup\n        )\n    \n    async def webapp_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):\n        \"\"\"Open the Mini-App.\"\"\"\n        keyboard = [[\n            InlineKeyboardButton(\n                \"🚀 Open Mini-App\",\n                web_app=WebAppInfo(url=WEBAPP_URL)\n            )\n        ]]\n        reply_markup = InlineKeyboardMarkup(keyboard)\n        \n        await update.message.reply_text(\n            \"Click the button to open the Mini-App:\",\n            reply_markup=reply_markup\n        )\n    \n    async def launch_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):\n        \"\"\"Launch a new token.\"\"\"\n        if not context.args or len(context.args) < 3:\n            await update.message.reply_text(\n                \"❌ Usage: `/launch <name> <symbol> <totalSupply>`\\n\\n\"\n                \"Example: `/launch DogeCoin DOGE 1000000000`\",\n                parse_mode='Markdown'\n            )\n            return\n        \n        name = context.args[0]\n        symbol = context.args[1]\n        try:\n            total_supply = int(context.args[2])\n        except ValueError:\n            await update.message.reply_text(\"❌ Total supply must be a number.\")\n            return\n        \n        await update.message.reply_text(\"⏳ Launching token...\")\n        \n        try:\n            response = await self.http_client.post(\n                f\"{MCP_SERVER_URL}/api/token/deploy\",\n                json={\n                    \"name\": name,\n                    \"symbol\": symbol,\n                    \"totalSupply\": total_supply,\n                    \"userId\": str(update.effective_user.id)\n                }\n            )\n            \n            if response.status_code == 200:\n                data = response.json()\n                await update.message.reply_text(\n                    f\"✅ *Token Launched!*\\n\\n\"\n                    f\"Name: {name}\\n\"\n                    f\"Symbol: {symbol}\\n\"\n                    f\"Supply: {total_supply:,}\\n\"\n                    f\"Contract: `{data.get('address', 'N/A')}`\\n\"\n                    f\"Tx: `{data.get('txHash', 'N/A')}`\",\n                    parse_mode='Markdown'\n                )\n            else:\n                await update.message.reply_text(\n                    f\"❌ Failed to launch token: {response.text}\"\n                )\n        except Exception as e:\n            logger.error(f\"Launch error: {e}\")\n            await update.message.reply_text(\n                f\"❌ Error launching token: {str(e)}\"\n            )\n    \n    async def trust_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):\n        \"\"\"Deploy a community trust.\"\"\"\n        if not context.args or len(context.args) < 2:\n            await update.message.reply_text(\n                \"❌ Usage: `/trust <name> <description>`\\n\\n\"\n                \"Example: `/trust MoonTrust Community trust for moon shots`\",\n                parse_mode='Markdown'\n            )\n            return\n        \n        name = context.args[0]\n        description = ' '.join(context.args[1:])\n        \n        await update.message.reply_text(\"⏳ Deploying trust...\")\n        \n        try:\n            response = await self.http_client.post(\n                f\"{MCP_SERVER_URL}/api/trust/deploy\",\n                json={\n                    \"name\": name,\n                    \"description\": description,\n                    \"userId\": str(update.effective_user.id)\n                }\n            )\n            \n            if response.status_code == 200:\n                data = response.json()\n                await update.message.reply_text(\n                    f\"✅ *Trust Deployed!*\\n\\n\"\n                    f\"Name: {name}\\n\"\n                    f\"Description: {description}\\n\"\n                    f\"Contract: `{data.get('address', 'N/A')}`\\n\"\n                    f\"Tx: `{data.get('txHash', 'N/A')}`\",\n                    parse_mode='Markdown'\n                )\n            else:\n                await update.message.reply_text(\n                    f\"❌ Failed to deploy trust: {response.text}\"\n                )\n        except Exception as e:\n            logger.error(f\"Trust error: {e}\")\n            await update.message.reply_text(\n                f\"❌ Error deploying trust: {str(e)}\"\n            )\n    \n    async def reserves_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):\n        \"\"\"View gold/silver reserves.\"\"\"\n        await update.message.reply_text(\"⏳ Fetching reserves...\")\n        \n        try:\n            response = await self.http_client.get(\n                f\"{MCP_SERVER_URL}/api/reserves\"\n            )\n            \n            if response.status_code == 200:\n                data = response.json()\n                await update.message.reply_text(\n                    f\"💰 *Reserve Statistics*\\n\\n\"\n                    f\"🥇 Gold: {data.get('gold', 0):,.2f} oz\\n\"\n                    f\"🥈 Silver: {data.get('silver', 0):,.2f} oz\\n\"\n                    f\"💵 Total Value: ${data.get('totalValue', 0):,.2f}\\n\"\n                    f\"📊 Ratio: {data.get('ratio', 'N/A')}\",\n                    parse_mode='Markdown'\n                )\n            else:\n                await update.message.reply_text(\n                    f\"❌ Failed to fetch reserves: {response.text}\"\n                )\n        except Exception as e:\n            logger.error(f\"Reserves error: {e}\")\n            await update.message.reply_text(\n                f\"❌ Error fetching reserves: {str(e)}\"\n            )\n    \n    async def portfolio_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):\n        \"\"\"Check user's portfolio.\"\"\"\n        await update.message.reply_text(\"⏳ Fetching portfolio...\")\n        \n        try:\n            response = await self.http_client.get(\n                f\"{MCP_SERVER_URL}/api/portfolio/{update.effective_user.id}\"\n            )\n            \n            if response.status_code == 200:\n                data = response.json()\n                tokens = data.get('tokens', [])\n                \n                if not tokens:\n                    await update.message.reply_text(\"📊 Your portfolio is empty.\")\n                    return\n                \n                portfolio_text = \"📊 *Your Portfolio*\\n\\